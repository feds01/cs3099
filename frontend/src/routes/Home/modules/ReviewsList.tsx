import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ReviewsList({}: Props): ReactElement {
    return (
        <div>
            reviews
        </div>
    )
}
